import 'package:flutter/material.dart';

class ExecucaoPage extends StatelessWidget {
  const ExecucaoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Execucao')),
    );
  }
}

